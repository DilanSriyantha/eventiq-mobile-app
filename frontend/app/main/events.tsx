import { StyleSheet, View } from "react-native";
import { Text } from "react-native-paper";

export default function Events() {
    return(
        <View>
            <Text variant="bodyLarge">Your Events</Text>
        </View>
    );
}

const styles = StyleSheet.create({

});